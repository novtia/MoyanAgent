import type { SettingsTab } from "./types";

export const TAB_TITLE_KEYS: Record<SettingsTab, string> = {
  appearance: "settings.tabAppearance",
  llm: "settings.tabLlm",
  default: "settings.tabDefault",
  search: "settings.tabSearch",
  system: "settings.tabSystem",
};
